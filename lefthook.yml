pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: npm run lint {staged_files}
    types:
      glob: "*.{ts,tsx}"
      run: npm run lint:ts
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
