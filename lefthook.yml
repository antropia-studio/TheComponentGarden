pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: npm run lint {staged_files}
    types:
      glob: "*.{js,ts, jsx, tsx}"
      run: npm run typecheck
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
